require 'spec_helper'
module Codebreak
  describe Game do
    describe 'win' do 
      it 'send congradulating message if win'
      it 'show ++++ and secret code'
      it 'show how many guesses take the win'
      it 'not win if guess code not the same as secret'
    end
  end
end

